
## 需求背景
当前项目爬取的 ShowDoc/云辉煌 文档存在格式不规范的问题,影响后续 RAG (检索增强生成) 系统的解析效果。需要在文档爬取流程中增加格式优化功能。

## 功能需求

### 1. 文档格式优化功能
**目标**: 优化下载的 Markdown 文档结构,使其符合 RAG 解析标准

**优化内容包括**:
- 规范化 Markdown 标题层级 (# ## ### 等)
- 修复错误的标题格式 (如标题前后多余空行、缺少空格等)
- 优化代码块格式 (添加语言标识、修复缩进)
- 规范化列表格式 (有序列表、无序列表)
- 清理多余的空行和空格
- 优化表格格式对齐
- 统一链接和图片引用格式
- 确保文档结构层次清晰

### 2. 执行时机
**关键节点**: 在文档采集完成后、生成 ZIP 压缩包之前执行

**具体位置**:
- 后端: 在 `run_crawler()` 和 `run_showdoc_crawler()` 函数中
- 时机: `crawler.crawl()` 执行完成后,`create_zip_file()` 调用之前
- 对所有已保存的 `.md` 文件进行格式优化处理

### 3. 实现方案

#### 后端实现 (Python)
1. 创建文档优化模块 `document_optimizer.py`
   - 实现 `DocumentOptimizer` 类
   - 提供 `optimize_markdown_file()` 方法优化单个文件
   - 提供 `optimize_directory()` 方法批量优化目录下所有 MD 文件

2. 集成到爬虫流程
   - 在 `app.py` 的 `run_crawler()` 和 `run_showdoc_crawler()` 中调用
   - 添加进度提示: "正在优化文档格式..."
   - 记录优化日志和统计信息

#### 前端展示 (可选)
1. 在任务进度条中显示优化阶段
2. 展示优化统计信息 (优化文件数量、修复问题数等)

### 4. 技术要点
- 使用正则表达式或 Markdown 解析库进行格式优化
- 确保优化过程不破坏原有内容的语义
- 处理异常情况,避免优化失败影响整体流程
- 保持优化前后文件编码一致 (UTF-8)

### 5. 验证标准
- 优化后的文档能被标准 Markdown 解析器正确解析
- 文档结构层次清晰,标题层级正确
- 代码块、列表、表格等格式规范
- 不丢失原有文档内容和信息

### 6. 交付物
- [x] `document_optimizer.py` - 文档优化核心模块
- [x] 更新 `app.py` - 集成优化功能到爬虫流程
- [x] 更新 `crawler.py` / `showdoc_crawler.py` - 支持优化回调
- [x] 测试用例 - 验证优化效果
- [x] 优化规则文档 - 记录优化规则和示例