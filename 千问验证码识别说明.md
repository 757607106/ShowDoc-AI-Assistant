# 千问大模型验证码识别配置说明

## 一、功能说明

ShowDoc API文档爬取系统已升级为使用**阿里云千问大模型(Qwen-VL-Max)**进行验证码识别,替代了原来的PaddleOCR服务。

### 优势对比

| 特性 | 千问大模型 | PaddleOCR |
|------|-----------|-----------|
| 识别准确率 | 更高(基于大模型理解) | 一般 |
| 部署复杂度 | 无需部署,API调用 | 需要部署或使用第三方服务 |
| 响应速度 | 快速 | 较快 |
| 稳定性 | 阿里云企业级服务 | 依赖第三方服务 |
| 灵活性 | 支持多模态,可自定义prompt | 固定OCR识别 |
| 成本 | 按调用量计费 | 依赖服务提供商 |

## 二、获取千问API Key

### 步骤1: 注册阿里云账号

1. 访问 [阿里云官网](https://www.aliyun.com/)
2. 点击右上角"注册"或"登录"
3. 完成账号注册(需要实名认证)

### 步骤2: 开通百炼平台

1. 访问 [阿里云百炼平台](https://dashscope.aliyun.com/)
2. 使用阿里云账号登录
3. 首次访问会提示开通服务,点击"立即开通"
4. 阅读并同意服务协议

### 步骤3: 创建API Key

1. 登录后进入控制台
2. 点击右上角头像 -> "API-KEY管理"
3. 点击"创建新的API-KEY"
4. 复制生成的API Key(格式类似: `sk-xxxxxxxxxxxxxxxx`)
5. **重要**: 妥善保管API Key,不要泄露给他人

### 步骤4: 查看余额和充值

1. 在百炼平台控制台查看"账户余额"
2. 如果余额不足,点击"充值"
3. 建议首次充值100元用于测试(实际消耗很少)

## 三、配置API Key

### 方法1: 环境变量(推荐)

**Linux/Mac**:

```bash
# 临时设置(当前会话有效)
export QWEN_API_KEY='sk-your-actual-api-key-here'

# 永久设置(添加到 ~/.bashrc 或 ~/.zshrc)
echo "export QWEN_API_KEY='sk-your-actual-api-key-here'" >> ~/.bashrc
source ~/.bashrc

# 或者对于zsh
echo "export QWEN_API_KEY='sk-your-actual-api-key-here'" >> ~/.zshrc
source ~/.zshrc
```

**Windows PowerShell**:

```powershell
# 临时设置
$env:QWEN_API_KEY="sk-your-actual-api-key-here"

# 永久设置(用户级)
[System.Environment]::SetEnvironmentVariable("QWEN_API_KEY", "sk-your-actual-api-key-here", "User")
```

**Windows CMD**:

```cmd
# 临时设置
set QWEN_API_KEY=sk-your-actual-api-key-here

# 永久设置(使用setx)
setx QWEN_API_KEY "sk-your-actual-api-key-here"
```

### 方法2: .env文件

1. 复制 `.env.example` 为 `.env`:
   ```bash
   cp .env.example .env
   ```

2. 编辑 `.env` 文件,填入真实的API Key:
   ```
   QWEN_API_KEY=sk-your-actual-api-key-here
   ```

3. 在代码中使用python-dotenv加载(需要先安装):
   ```bash
   pip install python-dotenv
   ```

4. 在app.py开头添加:
   ```python
   from dotenv import load_dotenv
   load_dotenv()
   ```

### 方法3: 直接传参(不推荐)

修改 `app.py` 中的初始化代码:

```python
# 不推荐:直接硬编码API Key
showdoc_auth = ShowDocAuthenticator(qwen_api_key='sk-your-api-key-here')
```

⚠️ **注意**: 直接硬编码API Key存在安全风险,不建议使用。

## 四、验证配置

### 检查环境变量是否设置成功

**Linux/Mac/PowerShell**:
```bash
echo $QWEN_API_KEY
```

**Windows CMD**:
```cmd
echo %QWEN_API_KEY%
```

### 运行测试

启动Flask应用:
```bash
python app.py
```

查看启动日志:
- ✅ 如果配置正确,不会有警告信息
- ❌ 如果未配置,会显示警告: `⚠️ 警告: 未设置QWEN_API_KEY环境变量`

## 五、使用说明

### 验证码识别流程

1. **获取验证码图片**: 系统自动从ShowDoc API获取
2. **转换为Base64**: 将图片二进制数据编码
3. **调用千问API**: 发送多模态请求(图片+文字提示)
4. **提取数字**: 从大模型响应中提取验证码数字
5. **提交登录**: 使用识别的验证码完成登录

### 识别准确率

- 正常情况下识别准确率 > 95%
- 失败时系统会自动重试,最多3次
- 重试机制确保登录成功率

### 费用说明

千问大模型调用费用非常低:
- Qwen-VL-Max模型价格约: 0.008元/千tokens
- 单次验证码识别消耗约 0.001-0.002元
- 100次识别约花费 0.1-0.2元

## 六、常见问题

### Q1: 提示"未设置QWEN_API_KEY环境变量"

**解决方案**:
1. 检查是否正确设置了环境变量
2. 确保在启动Flask之前设置
3. 重启终端窗口使环境变量生效

### Q2: API Key无效或认证失败

**解决方案**:
1. 检查API Key是否复制完整(包括`sk-`前缀)
2. 确认API Key未被撤销或过期
3. 在百炼平台控制台检查API Key状态

### Q3: 余额不足

**解决方案**:
1. 登录百炼平台查看账户余额
2. 充值后继续使用
3. 单次验证码识别费用很低,100元可用很久

### Q4: 识别失败

**可能原因**:
1. 网络连接问题,无法访问阿里云
2. API Key配置错误
3. 验证码图片质量问题

**解决方案**:
1. 检查网络连接
2. 验证API Key配置
3. 系统会自动重试3次
4. 查看控制台日志获取详细错误信息

### Q5: 响应速度慢

**原因**:
- 大模型推理需要一定时间
- 网络延迟

**正常范围**:
- 单次识别响应时间: 2-5秒
- 超时时间设置: 30秒

### Q6: 如何查看API调用日志

**方法**:
1. 在百炼平台控制台可以查看API调用统计
2. 查看Flask应用控制台输出
3. 启用DEBUG模式查看详细日志

## 七、技术细节

### API请求示例

```python
import requests
import base64

# 读取验证码图片
with open('captcha.png', 'rb') as f:
    image_data = f.read()

# 转换为Base64
image_base64 = base64.b64encode(image_data).decode('utf-8')

# 构造请求
headers = {
    "Authorization": f"Bearer {QWEN_API_KEY}",
    "Content-Type": "application/json"
}

payload = {
    "model": "qwen-vl-max",
    "input": {
        "messages": [
            {
                "role": "user",
                "content": [
                    {"image": f"data:image/png;base64,{image_base64}"},
                    {"text": "这是一个验证码图片,请识别图片中的数字并直接返回数字,不要有任何其他文字。"}
                ]
            }
        ]
    },
    "parameters": {
        "result_format": "message"
    }
}

# 发送请求
response = requests.post(
    "https://dashscope.aliyuncs.com/api/v1/services/aigc/multimodal-generation/generation",
    json=payload,
    headers=headers
)

# 解析响应
result = response.json()
content = result["output"]["choices"][0]["message"]["content"]
```

### 模型选择

当前使用 `qwen-vl-max` 模型:
- 视觉理解能力最强
- 识别准确率最高
- 支持中英文混合识别
- 适合验证码等复杂场景

### Prompt优化

当前使用的提示词:
> "这是一个验证码图片,请识别图片中的数字并直接返回数字,不要有任何其他文字。"

**优化建议**:
- 明确指出是验证码
- 要求只返回数字
- 不要额外的解释文字

## 八、迁移指南

### 从PaddleOCR迁移

已完成的修改:
1. ✅ `showdoc_auth.py`: 更新`recognize_captcha`方法
2. ✅ `app.py`: 添加API Key配置
3. ✅ 文档更新: README和设计文档

无需修改的部分:
- ✅ 验证码获取逻辑
- ✅ 登录流程
- ✅ 其他业务逻辑

### 回退方案

如需回退到PaddleOCR:
1. 恢复 `showdoc_auth.py` 中的旧代码
2. 恢复 `app.py` 中的初始化逻辑
3. 无需API Key配置

## 九、相关链接

- [阿里云百炼平台](https://dashscope.aliyun.com/)
- [千问大模型文档](https://help.aliyun.com/zh/dashscope/developer-reference/api-details)
- [API-KEY管理](https://dashscope.console.aliyun.com/apiKey)
- [计费说明](https://help.aliyun.com/zh/dashscope/developer-reference/tongyi-qianwen-vl-plus-api)

---

**配置完成后即可开始使用!** 🎉
